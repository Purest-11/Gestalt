# ═══════════════════════════════════════════════════════════════════
# 🌐 公网隧道模式 - Docker Compose 覆盖配置
# 
# 使用方法：
#   docker-compose -f docker-compose.yml -f docker-compose.tunnel.yml up -d
#
# 此配置会添加 Cloudflare Tunnel，让观众可以通过公网访问
# ═══════════════════════════════════════════════════════════════════

version: '3.8'

services:
  # ═══════════════════════════════════════════════════════════════
  # 🌐 Cloudflare Tunnel（公网入口）
  # ═══════════════════════════════════════════════════════════════
  tunnel:
    image: cloudflare/cloudflared:latest
    container_name: osc-tunnel
    restart: unless-stopped
    command: tunnel --no-autoupdate --url http://audience:3002
    networks:
      - osc-network
    depends_on:
      audience:
        condition: service_healthy

# ═══════════════════════════════════════════════════════════════
# 网络配置（与主配置合并，无需重复定义）
# ═══════════════════════════════════════════════════════════════
networks:
  osc-network:
    # 使用主配置中定义的网络

# ═══════════════════════════════════════════════════════════════
# 使用说明
# ═══════════════════════════════════════════════════════════════
#
# 🚀 启动公网模式：
#   docker-compose -f docker-compose.yml -f docker-compose.tunnel.yml up -d
#
# 📡 查看公网URL（重要！）：
#   docker-compose logs tunnel 2>&1 | grep -o 'https://[a-zA-Z0-9-]*\.trycloudflare\.com'
#
#   或者实时查看：
#   docker-compose logs -f tunnel
#
# 🛑 停止：
#   docker-compose -f docker-compose.yml -f docker-compose.tunnel.yml down
#
# 💡 观众访问：
#   公网URL会显示在日志中，类似：
#   https://random-words-1234.trycloudflare.com
#
#   观众入口：
#   https://random-words-1234.trycloudflare.com/audience-touch/
#
# ⚠️ 注意事项：
#   - 每次启动隧道，URL 都会变化（除非使用 Cloudflare 账号配置固定域名）
#   - 隧道需要几秒钟时间建立连接
#   - 如果 audience 服务未就绪，隧道会等待
#
# ═══════════════════════════════════════════════════════════════
